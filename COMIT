-c        grep -c "error" /var/log/syslog       # подсчитать количество совпадений
-e        grep -e "linux$" filename             # использовать регулярные выражения (строки, заканчивающиеся на "Linux")
-f        grep -f pattern_file.bashrc           # брать шаблоны (PATTERNS) из файла
-i        grep -i "Alias" .bashrc               # игнорировать регистр при поиске
-l        grep -l "alias" /home/test/*          # вывести только имена файлов, в которых найдены совпадения
-L        grep -L "alias" /home/test/*          # вывести только имена файлов, в которых нет совпадений
-r        grep -r "error" /var/log/log.txt      # рекурсивный поиск (внутри подкаталогов)
-v        grep -v "warning" /home/test/log.txt  # выбирать строки, не содержащие совпадение
-m        grep -m 3 "alias" .bashrc             # ограничить вывод результатов до 3 строк
-n        grep -n "sudo" .bashrc                # вывести номера строк с совпадениями
-o        grep -o "search string" .bashrc       # показывать только ту часть строки, которая совпадает с шаблоном
-x        grep -x "linux is love" filename      # сопоставлять только целые строки
-w        grep -w "alias" .bashrc               # сопоставлять только целые слова
-A        grep -A 6 "error" error.log           # вывести 6 строк после совпадения
-B        grep -B 2 "warning" error.log         # вывести 2 строки перед совпадением
-C        grep -C 7 "info" error.log            # вывести 7 строк до и после совпадения
-E        grep -E "b(a|e)g" filename            # расширенные регулярные выражения (строки, содержащие "bag" или "beg")


ls ~ | grep "^s"        # найдем строки, которые начинаются на символ "s"
ls ~ | grep "\.txt$"    # найдем все файлы с расширением "txt"
grep "T.m" test.txt     # Найдем в файле test.txt все строки, которые содержат "Tim" или "Tom"
grep "T[ou]m" test.txt      # найдем все строки, которые содержат "Tum" или "Tom"
grep "T[^ou]m" test.txt     # найдем все строки, которые НЕ содержат "Tum" или "Tom"
grep "^0-9" test.txt        # найдем все строки, которые начинаются с цифрового символа
ls ~/Downloads | grep "\.tar\.gz"       # найдем в папке загрузок все файлы, которые имеют расширение tar.gz
grep "o\{2\}" test.txt      # найдем строки, где есть два буквы o подряд

grep "pattern" file.txt             # Искать шаблон в файле
grep "example" *.txt                # поиск строки "example" во всех файлах с расширением ".txt"
grep -i "pattern" file.txt          # Поиск без учёта регистра
grep -v "pattern" file.txt          # Инвертировать совпадение - показать строки, которые не соответствуют шаблону
grep -c "pattern" file.txt          # Посчитать количество строк, соответствующих шаблону
grep -l "pattern" *.txt             # Вывести имена файлов, содержащих шаблон
grep -L "pattern" *.txt             # Вывести имена файлов, которые не содержат шаблон
grep -w "word" file.txt             # Искать только целые слова
grep -x "exact line" file.txt       # Искать строки, полностью совпадающие с шаблоном
grep -o "pattern" file.txt          # Выводить только совпадающие части строк
grep -n "pattern" file.txt          # Выводить номера строк с совпадениями
grep -A 3 "pattern" file.txt        # Выводить 3 строки после каждого совпадения
grep -B 3 "pattern" file.txt        # Выводить 3 строки перед каждым совпадением
grep -C 3 "pattern" file.txt        # Выводить 3 строки до и после каждого совпадения
grep -E "regex" file.txt            # Использовать расширенные регулярные выражения
grep -F "fixed string" file.txt     # Искать фиксированную строку (без использования регулярных выражений)
grep -P "perl_regex" file.txt       # Использовать Perl-совместимые регулярные выражения
grep --include="*.log" "error" /var/log/ # Искать только в файлах, соответствующих шаблону (например, *.log)
grep --exclude="*.bak" "pattern" *   # Исключить из поиска файлы, соответствующие шаблону (например, *.bak)
grep --exclude-dir="backup" "pattern" * # Исключить из поиска каталоги, соответствующие шаблону (например, "backup")
grep -r "pattern" /path/            # Рекурсивно искать в каталогах (следует символические ссылки)
grep -R "pattern" /path/            # Рекурсивно искать, следуя символическим ссылкам
grep -Z "pattern" file.txt          # Выводить результаты, разделяя их нуль-байтами
grep --color=auto "pattern" file.txt # Выделять совпадения цветом

grep -i "example" my.txt        # поиск строки "example" без учёта регистра букв
grep -c "example" my.txt        # подсчёт количества строк, содержащих "example"
grep -n "example" my.txt        # вывод номеров строк вместе с найденными совпадениями
grep -r "example".              # рекурсивный поиск строки "example" во всех файлах (включая поддиректории)
grep -v "example" my.txt        # вывод строк, которые не содержат "example"
grep -w "example"                # поиск строк, содержащих "example" как отдельное слово
grep -e "key1" -e "key2" my.txt        # вывод строк, содержащих либо "key1", либо "key2"
grep -v -e "key1" -e "key2" my.txt        # вывод строк, которые не содержат ни "key1", ни "key2"
grep "key1*key2" my.txt                 # вывод строк, содержащих одновременно "key1" и "key2"
grep -E "error|warning" app.log        # использование расширенных регулярных выражений для поиска строк "error" или "warning" в файле "app.log"
grep -E "^[a-zA-Z]" my.txt              # ещё один пример использования расширенных регулярных выражений (поиск строк, начинающихся с латинской буквы)
grep -m3 "keyword" my.txt               # ограничение вывода команды grep до трёх строк с совпадениями
grep -A2 -B2 "example" my.txt        # вывод двух строк до и двух строк после найденного совпадения
grep -C3 "error" server.log         # вывод трёх строк до и трёх строк после строки с "error" в файле "server.log"
grep -x "spoofing" my.txt           # вывод строк, которые полностью совпадают с "spoofing"
grep -l "example" *             # вывод имён файлов, в которых найдено совпадение с "example"
grep "^hello" my.txt            # вывод всех строк, начинающихся со слова "hello"
grep "done$" my.txt             # вывод всех строк, заканчивающихся на "done"
grep -o "begin.*end" my.txt     # вывод только совпадающей части строки (между "begin" и "end")
grep --color "example" my.txt   # выделение найденных совпадений цветом
grep "[0-9]" my.txt             # вывод всех строк, содержащих цифры
		grep -a "string" my.bin         #поиск строки "string" в бинарном файле "my.bin"

awk '{print $0}' file.txt                           # Вывести каждую строку файла
awk '/pattern/ {print $0}' file.txt                # Вывести строки, соответствующие шаблону
awk '{print $1}' file.txt                          # Вывести первый столбец каждой строки
awk '{print $1, $3}' file.txt                      # Вывести первый и третий столбцы каждой строки
awk 'BEGIN {print "Header"} {print $0} END {print "Footer"}' file.txt # Добавить заголовок и подвал